# Tennessee Local Rules of Practice (per-county index) — Per-county local rules — typography, page limits, chambers copies, motion-docket scheduling

> **Source:** https://www.tncourts.gov/courts/court-rules2/local-rules-practice
> **Fetched:** 2026-07-01
> **Status:** _(stub — verbatim text not retrieved)_ — stub-only target (no per-rule HTML index)
> **Format:** pointer stub

> **NOT LEGAL ADVICE.** This file is a pointer to the canonical source; verify against the source itself before filing.

---

## Scope

Per-county local rules — typography, page limits, chambers copies, motion-docket scheduling.

## How to retrieve verbatim text

Open the canonical URL above in a browser, or re-run `scripts/pull_tn_court_rules.py --only Tenn-Local-Rules-Practice` to retry. The Tennessee AOC publishes this rule set at `tncourts.gov`; no authentication is required.

## Notes

Local rules are published one set per county by each clerk and are indexed at the AOC 'Local Rules of Practice' page. They are not aggregated into a single rule-set page that this puller can walk. Confirm the specific county's current local rules at the AOC index and fetch the PDF for the filing court.
